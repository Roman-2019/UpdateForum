@model List<string>
@{
    ViewBag.Title = "GetActiveUserRole";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Роли текущего юзера</h2>

@foreach (var item in Model)
{
    <p>@item</p>
    <p>@ViewBag.ActiveUserRole</p>
}
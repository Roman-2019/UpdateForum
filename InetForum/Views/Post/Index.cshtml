@model IEnumerable<InetForum.Models.PostViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<h2>Index</h2>*@

<h2>List of all Posts</h2>

<table class="table">
    <tr>
        <th>Category</th>
        <th>Title post</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryViewModel.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.RouteLink("Go to details", new { controller = "Comment", action = "CommentByPost", id = item.Id })
            </td>
        </tr>
    }
</table>

@if (@ViewBag.ActiveUserRole[0]=="admin") 
{
<p>
    @Html.ActionLink("Create New", "Create")
</p>
}
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuthorViewModel.NickName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryViewModel.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTime)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AuthorViewModel.NickName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryViewModel.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateTime)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
@for (int i=0;i<@ViewBag.ActiveUserRole.Count;i++)
{
    <p>@ViewBag.ActiveUserRole[i]</p>
}